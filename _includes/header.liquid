<header class="flex justify-between">

    {% if editorSettings.headerTitle.uppercaseGradient %}
    <div class="logo uppercase-gradient">
        {% if page.url == "/" %}
        <h1 >
            <a href="/" class="text-6xl font-bold uppercase leading-none">Raphaël Ferrand</a>
        </h1>
        {% else %}
            <a href="/" class="text-6xl font-bold uppercase leading-none">Raphaël Ferrand</a>
        {% endif %}
    </div>
    {% endif %}

    {% if editorSettings.headerTitle.smudgedVioletGaramond %}
    <div class="logo">
        <a href="/" class="text-6xl font-bold uppercase leading-none flex items-center">
            <img src="/img/s-logo-pink.svg" alt="Raphaël Ferrand" class="hidden md:block w-32 md:w-20 mr-4">
            {% if page.url == "/" %}
            <p class="text-2xl md:text-3xl title-text">Raphaël Ferrand</p>
            {% else %}
            <p class="text-lg md:text-3xl  title-text">Raphaël Ferrand</p>
            {% endif %}
        </a>
    </div>
    {% endif %}
    
    {% include topnav %}
</header>